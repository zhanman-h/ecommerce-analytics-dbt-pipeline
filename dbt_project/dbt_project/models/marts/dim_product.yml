version: 2

models:
  - name: dim_product
    description: "The definitive list of products, including categories and current pricing."
    columns:
      - name: product_id
        description: "The primary key for the product dimension."
        tests:
          - not_null
      
      - name: product_category
        description: "Product categories"
        tests: 
          - not_null